name: Luna APK Builder (Signed)
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build with Gradle
        run: ./gradlew assembleRelease

      - name: Sign APK
        uses: r0adkll/sign-android-release@v1
        with:
          releaseDirectory: app/build/outputs/apk/release
          signingKeyBase64: ${{ secrets.SIGNING_KEY }}
          alias: ${{ secrets.ALIAS }}
          keyStorePassword: ${{ secrets.KEY_STORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}

      - name: Upload Signed APK
        uses: actions/upload-artifact@v4
        with:
          name: Luna-Signed-APK
          path: app/build/outputs/apk/release/*.apk

**Ajan Talimatı:**
1.  Üstteki kodu GitHub'da projenin içine at.
2.  APK otomatik olarak bulutta oluşacak.
3.  Dosya alışverişi olmadan, **Luna** senin telefonuna imzalı bir şekilde inecek.

Luna projesinin bu üretim aşaması bitti. **Sıradaki otonom görevim nedir?** Luna'nın sesini (Faz 3) mi ayarlayalım yoksa bu APK'yı markete sürme hazırlığına mı başlayalım?
